server:
  port: 8088
spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/feedback_db}
    username: ${DB_USER:admin}
    password: ${DB_PASS:secret}
  jpa:
    hibernate:
      ddl-auto: update


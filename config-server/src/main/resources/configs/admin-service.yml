server:
  port: 8082
spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/admin_db}
    username: ${DB_USER:admin}
    password: ${DB_PASS:secret}
  jpa:
    hibernate:
      ddl-auto: update

